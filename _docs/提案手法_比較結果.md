# 提案手法（my_method）比較結果

このドキュメントは、`my_method/` の提案手法とベースラインを **同条件** で比較した結果を記録する。

---

## 比較ポリシー（重要）

- **揃えるもの**: `epochs`, `early_stopping`, `batch_size`, `scheduler`, `lr`, `amp/tf32`, `grad_clip`, `lambda_shot` など
- **変えるもの**: 提案要素のみ（例: `lambda_consistency`）

※ `consistency_stable`（AMP/TF32 off, lr=0.0005）と比較する baseline は、`baseline_stable` を使用する（同条件）。

---

## 実験結果サマリ（Test）

| 系統 | config / run | early stop | Receiver Top1 | Receiver Top3 | Shot Acc | Shot AUC-ROC | Shot F1 | 備考 |
|---|---|---:|---:|---:|---:|---:|---:|---|
| tacticai | `runs/receiver_shot/training_20251224_084417.log` | 31 | 0.2105 | 0.4912 | 0.5263 | 0.4599 | 0.6897 | 既存 baseline（D2） |
| my_method | `configs_my_method/multitask_receiver_shot_d2_consistency_stable.yaml` / `consistency_stable` | 45 | **0.3333** | **0.5088** | 0.5263 | **0.5778** | 0.6897 | 提案（安定性優先設定で完走） |
| my_method | `configs_my_method/multitask_receiver_shot_d2_baseline_stable.yaml` / `baseline_stable` | - | - | - | - | - | - | **要実行（同条件baseline）** |

---

## 実行コマンド（同条件baseline）

```bash
python -m my_method.train.train_multitask --config configs_my_method/multitask_receiver_shot_d2_baseline_stable.yaml
```

完了後、`runs/my_method/receiver_shot/baseline_stable/final_training_history_*.csv` から Test 行を読み取って、上表の baseline 行を埋める。


